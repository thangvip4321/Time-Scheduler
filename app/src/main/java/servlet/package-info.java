/**
 * The servlet package contains code for Java servlets for processing HTTP requests.
 * <p>
 * Each servlet has its own endpoint andis only referenced when a corresponding request is sent to the 
 * server
 *
 * @since 1.0
 */
package servlet;
