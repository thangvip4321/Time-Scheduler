/**
 * The servlet package contains code for Java servlets for processing HTTP requests.
 * <p>
 * Each servlet has its own endpoint and is only called when a corresponding request is sent to the 
 * server.The mapping of requests to servlet is done automatically by Tomcat and can be configured by changing the{@code web.xml} file.
 *
 * @since 1.0
 * @author Nguyen Duc Thang
 */
package servlet;
